apply plugin: 'maven-publish'

jar {
    enabled = true
}

publishing {
    publications {
        maven(MavenPublication) {
            artifactId = 'server-common'
            version = "$version"

            from components.java
        }
    }
}