apply plugin: 'maven-publish'

jar {
    enabled = true
}

publishing {
    publications {
        maven(MavenPublication) {
            groupId = 'io.microconfig'
            artifactId = 'server-common'
            version = '0.1.0'

            from components.java
        }
    }
}